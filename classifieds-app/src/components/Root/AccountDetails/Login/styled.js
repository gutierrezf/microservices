import styled from "styled-components";

const LogginButton = styled.button`
  display: inline-block;
  margin-top: 0.25rem;
`;

const SignUpBlock = styled.div`
  display: inline-block;
  margin-top: 0.25rem;
  font-size: 0.9rem;
`;
export { LogginButton, SignUpBlock };
